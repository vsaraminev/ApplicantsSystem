@using ApplicantsSystem.Models
@using Microsoft.AspNetCore.Identity

@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="navbar-nav navbar-right">
            <li class="nav-item active">
                <a class="nav-link active" title="Manage">Hello @UserManager.GetUserName(User).Split('@').First()</a>
            </li>
            <li>
                <button type="submit" class="btn btn-link nav-link"><strong>Logout</strong></button>
            </li>            
        </ul>
    </form>
}
@*else
{
    <ul class="nav navbar-nav navbar-right">
        <li class="nav-item">
            <a asp-area="Identity" asp-page="/Account/Login" class="nav-link">Login</a>
        </li>
    </ul>
}*@